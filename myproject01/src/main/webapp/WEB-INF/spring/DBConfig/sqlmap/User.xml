<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<!-- 맵핑될 DAO 인터페이스의 Full name 을 줍니다. --> 

<mapper namespace="UserCheckMapper"> 
    <!-- 로그인 체크 -->
    <!-- select id="UserInfoCheck" parameterType="hashMap" resultType="UserVO">
		SELECT USER_ID 
		      ,USER_PW 
		      ,NAME 
		      ,EMAIL 
		      ,BIRTH 
		      ,PHONE 
		  FROM GUSER  
		 WHERE Email = #{Email}
		   AND USER_PW = #{password} 
    </select--> 
    
    <!-- select id="UserCheckEmail" parameterType="hashMap" resultType="UserVO">
		SELECT USER_ID 
		      ,USER_PW 
		      ,NAME 
		      ,EMAIL 
		      ,BIRTH 
		      ,PHONE 
		  FROM GUSER  
		 WHERE Email = #{Email} 
    </select-->           
    
</mapper>